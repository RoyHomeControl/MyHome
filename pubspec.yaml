name: myhome
description: Remote control my home
version: 1.0.2+9

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0
  crypto: ^3.0.6
  path_provider: ^2.1.5
  downloadsfolder: ^1.2.0
  flutter_app_installer: ^2.0.0

flutter:
  uses-material-design: true